# Third-party notices

The npm package bundles its JavaScript dependencies. Their license declarations and source locations are recorded in `package-lock.json` and the corresponding npm package metadata. The main bundled dependencies are Commander (MIT), Inquirer and its packages (MIT), string-width and its packages (MIT), cross-spawn (MIT), and semver (ISC).
